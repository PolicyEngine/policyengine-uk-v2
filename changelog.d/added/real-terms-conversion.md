Add nominal-to-real conversion to the Python wrapper: a `deflate(nominal, nominal_year, base_year)` helper and a canonical CPI index (rebased to 2010/11 = 100) in a new `realterms` module, kept in sync with `cpi_index_for_year` in `clean.rs`. `SimulationResult` gains `real_factor(base_year)` and `real_hbai_incomes(base_year)` so HBAI incomes can be compared across years in constant prices.
