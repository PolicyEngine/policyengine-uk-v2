Surface per-class National Insurance on `PersonResult` and microdata exports: new fields `ni_class1_employee`, `ni_class2`, `ni_class4` (summing to the existing `national_insurance` total) plus matching `baseline_/reform_ni_class*` columns in the persons CSV and JSON record outputs. Existing `baseline_employee_ni` / `baseline_employer_ni` columns and the `national_insurance` field are unchanged, so existing consumers continue to work. Lays substrate for downstream notebooks that need Class-level granularity (salary-sacrifice cap, NIC exemption for disabled employees, employer-NI scoring) — splitting `NationalInsuranceParams` itself is a follow-up slice of #43.
