Add a `Simulation::branch(parameters)` primitive that forks a baseline simulation under a counterfactual `Parameters`, reusing the entity frame and carrying through the original baseline-old-SP rate. Companion `Comparison::between(baseline, reform)` produces the per-household net-income deltas, plus aggregate `revenue_change`, `benefit_spending_change`, `net_cost`, and winners/losers/unchanged shares, with sign convention matching `BudgetaryImpact.revenue_change`. Lays substrate for axes (sweep over a range) and dynamics (re-run downstream after a behavioural-response edit) — first slice of #49; both follow up.
