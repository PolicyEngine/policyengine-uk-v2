Add a Criterion benchmark (`benches/branch.rs`) for the `Simulation::branch` + `Comparison` primitive, comparing the fork against the existing `main.rs` clone-then-`new` construction, the heavy `run()`/`Comparison::between` components, and the full baseline→reform→compare pipeline both ways. Self-contained on a synthetic frame (scale via `BRANCH_BENCH_HH`, default 20k households); run with `cargo bench --bench branch`. Confirms `branch` adds no measurable overhead over the dance — both are dominated by the unavoidable entity-frame clone the binary already pays.
