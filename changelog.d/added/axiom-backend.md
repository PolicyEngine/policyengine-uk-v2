Add the first slice of the axiom rules-engine backend (`src/axiom/`): policyengine-uk-rust can now evaluate statute-derived RuleSpec rules (composed from rulespec-uk) instead of hand-coded variable logic. `Policy` wraps a compiled artifact; reforms and future-dated uprating assumptions are parameter overrides (`Policy::with_parameter`) that patch the versioned parameter table and recompile in memory in well under a millisecond. `Dataset` maps per-person input columns onto one tax year, and `calculate` runs the engine's dense columnar executor, returning output columns in person order. Ships a compiled income tax artifact (ITA 2007 s.10 + s.35, FA 2026 s.2 rates) and a reform demo bench (`cargo bench --bench axiom_reform`): a basic-rate +1p impact over 100k people — baseline eval, parameter patch, recompile, reform eval — runs end to end in under 40 ms. Coverage is the s.10 rate-band surface only for now; the full s.23 liability composition is the next slice.
