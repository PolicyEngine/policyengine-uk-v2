Add residential-property surcharge to CGT: new `Person.capital_gains_residential_share` field (0.0–1.0; default 0.0 — entire gain treated as non-residential) and new `CapitalGainsTaxParams.residential_surcharge` parameter (default 0.0, matching post-April-2025 unified rates). The surcharge is added on top of the basic / higher rate for the residential slice of taxable gains, after the AEA is applied pro-rata. Lets reforms model re-introducing the historical residential surcharge (8 pp / 28 % at higher rate, pre-April-2024). Unchanged behaviour for existing baseline: zero share + zero surcharge yields the same CGT as before. Addresses part of #45 (CGT residential surcharge); behavioural elasticity and pensions-tax port remain follow-up slices.
