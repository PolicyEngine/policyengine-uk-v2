Split the monolithic `calculate_universal_credit` into named element functions: `uc_standard_allowance_monthly`, `uc_child_element_monthly`, `uc_disabled_child_element_monthly`, `uc_lcwra_element_monthly`, `uc_carer_element_monthly`, `uc_housing_element_monthly`, `uc_work_allowance_annual`, `uc_net_earned_income`, `uc_unearned_income`, plus `uc_has_working_age_adult` and `uc_has_lcwra` predicates. Each element carries the relevant UC Regs 2013 reference in a doc-comment. Pure refactor — orchestrating function still produces identical totals (verified against 135 existing tests). Adds 5 element-level unit tests covering standard-allowance bands, two-child limit, zero-children case, and work-allowance gating. Lays substrate for further per-element work toward #40 (childcare element, transitional protection, capital limits remain follow-up slices).
