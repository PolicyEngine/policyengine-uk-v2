Add `scripts/calibration_audit.py`, a revenue / spending calibration audit that runs a full FRS simulation and compares each program total against OBR targets loaded from `scripts/obr_<year>_<year+1>.yaml` (initial OBR snapshot for 2025/26 included). Prints engine / OBR / ratio columns, flags programs drifting more than 15% from OBR, and exits non-zero when drift exceeds tolerance — supports tracking progress on the calibration punch list in #13.
