Remove warm-start weight initialisation from the EFRS calibration pipeline. The warm-start existed to counter Adam's null-space non-determinism; with a deterministic optimiser it is unnecessary. `warm_start_weights()` and both call sites in `efrs.py` are removed; the `start_weight` column handling is removed from `calibrate.py`.
