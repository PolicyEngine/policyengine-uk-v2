Replace Adam log-space optimiser in `data/calibrate.py` with the CALMAR logit Newton-Raphson method, matching the DWP PSM calibration approach. Weights are bounded within [L, U] × w0 via the logit function (L=0.05, U=10 by default) with constraint satisfaction enforced exactly through NR on the Lagrange multipliers, rather than approximately through gradient descent with a post-step clamp. `CalibrateConfig` drops the Adam-specific fields (`epochs`, `lr`, `beta1`, `beta2`, `eps`, `dropout`) and replaces them with `logit_l`, `logit_u`, and `max_iter`. Warm-start logic from prior-year calibrated weights is preserved.
