# OBR fiscal-year 2025/26 forecast values (£ billions, gross of policy changes
# announced after this snapshot was taken). Used by scripts/calibration_audit.py
# to compare engine totals against published OBR figures.
#
# Source: figures lifted from the first-pass calibration table posted in
# https://github.com/PolicyEngine/policyengine-uk-rust/issues/13. Update this
# file as fresher OBR EFO/OBR-stat releases land. Each entry maps a key from
# `policyengine_uk_compiled.ProgramBreakdown` (or a derived combination) to its
# OBR target.
#
# Tolerance: programs are flagged in the audit table when |1 - engine/OBR|
# exceeds 0.15 (15 %), matching the threshold in the issue body.

income_tax:        303.0
employee_ni:        60.0
employer_ni:       120.0
vat:               180.0
fuel_duty:          24.0
alcohol_duty:       13.0
tobacco_duty:        8.0
capital_gains_tax:  15.0
stamp_duty:         15.0
council_tax:        46.0
hicbc:               0.7
state_pension:     140.0
child_benefit:      13.0
universal_credit:   75.0
pension_credit:      5.4
housing_benefit:    15.0
carers_allowance:    4.2
